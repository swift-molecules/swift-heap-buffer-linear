# swift-heap-buffer-linear

Integration of the Heap atom with the linear buffer column stack (`Buffer<Storage<Memory.Allocator<...>>.Contiguous<E>>.Linear`), including the canonical `Heap<E>` front door.
